<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SongsMainActivity"
            android:label="@string/songs_activity_name">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NowPlayingActivity"
            android:label="@string/now_playing_activity_name"
            android:parentActivityName=".SongsMainActivity" />
        <activity
            android:name=".AlbumsActivity"
            android:label="@string/albums_activity_name" />
        <activity
            android:name=".ArtistsActivity"
            android:label="@string/artists_activity_name" />
    </application>

</manifest>